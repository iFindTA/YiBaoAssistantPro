CREATE TABLE IF NOT EXISTS t_author_usr (
uid TEXT NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
imid TEXT DEFALUT NULL,
acc TEXT DEFAULT NULL,
pwd TEXT DEFAULT NULL,
authorToken TEXT DEFAULT NULL,
authorStamp INTEGER DEFAULT 0,
soundable TEXT DEFAULT 1,
shakeable TEXT DEFAULT 1,
soundfile TEXT DEFAULT 'in.caf',
autologin TEXT DEFAULT 1,
ext TEXT DEFAULT NULL
)|
CREATE TABLE IF NOT EXISTS t_usrs (
uid TEXT NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
age TEXT DEFAULT 0,
gender TEXT DEFAULT NULL,
nick TEXT DEFAULT NULL,
avatar TEXT DEFAULT NULL,
signature TEXT DEFAULT NULL,
authorName TEXT DEFAULT NULL,
authorID TEXT DEFAULT NULL,
authorType TEXT DEFAULT 0,
ext TEXT DEFAULT NULL
)|
CREATE TABLE IF NOT EXISTS t_patients(
pid TEXT NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
pavatar TEXT DEFAULT NULL,
puid TEXT DEFAULT NULL,
puname TEXT DEFAULT NULL,
puascii TEXT DEFAULT NULL,
pHRecNum TEXT DEFAULT NULL,
isMem TEXT DEFAULT 0,
memType TEXT DEFAULT 0,
memTypeName TEXT DEFAULT NULL,
ownerid TEXT DEFAULT NULL,
ext TEXT DEFAULT NULL
)|
CREATE TABLE IF NOT EXISTS t_inquirers (
iid TEXT NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
iname TEXT DEFAULT NULL,
iavatar TEXT DEFAULT NULL,
iascii TEXT DEFAULT NULL,
iphone TEXT DEFAULT NULL,
itype TEXT DEFAULT NULL,
iPnum TEXT DEFAULT NULL,
ownerid TEXT DEFAULT NULL,
ext TEXT DEFAULT NULL
)|
CREATE TABLE IF NOT EXISTS t_quick_reply (
id INTEGER NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
qtitle TEXT DEFAULT NULL,
qtype TEXT DEFAULT NULL,
qcontent TEXT DEFAULT NULL,
ownerid TEXT DEFAULT NULL
)|
CREATE TABLE IF NOT EXISTS t_msg_session (
sessionId TEXT NOT NULL PRIMARY KEY UNIQUE ON CONFLICT REPLACE,
dateTime INTEGER DEFAULT 0,
type INTEGER DEFAULT 0,
text TEXT DEFAULT 0,
unreadCount INTEGER DEFAULT 0,
sumCount INTEGER DEFAULT 0,
conversationId varchar(128),
assistantUserId varchar(128),
customerUserId varchar(128),
replyStatus INTEGER DEFAULT 0,
lastMsgUserData varchar(2048),
sendState INTEGER DEFAULT 0,
sticky INTEGER DEFAULT 0,
ownerid TEXT DEFAULT NULL
)